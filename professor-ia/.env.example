# ===================================
# Professor IA - Variáveis de Ambiente
# ===================================

# Chave secreta para sessões (gere uma aleatória!)
# No terminal: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=troque-por-uma-chave-secreta-aleatoria

# Chave global do Gemini (opcional)
# Se preenchida, os alunos não precisam configurar a própria chave.
# Obtenha grátis em: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Porta do servidor (padrão: 5000)
PORT=5000
